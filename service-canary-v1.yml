apiVersion: v1
kind: Service
metadata:
  name: canary-v1-service
  labels:
    app: chat
    version: v1
spec:
  selector:
    app: chat
    version: v1
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
