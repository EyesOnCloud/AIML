apiVersion: v1
kind: Service
metadata:
  name: chat-app
  namespace: chat-app
  labels:
    app: chat-app
spec:
  selector:
    app: chat-app
  type: NodePort
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
