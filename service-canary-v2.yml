apiVersion: v1
kind: Service
metadata:
  name: canary-v2-service
  labels:
    app: chat
    version: v2
spec:
  selector:
    app: chat
    version: v2
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
